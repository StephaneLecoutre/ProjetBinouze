{% extends 'base.html.twig' %}

{% block title %}Recipes{% endblock %}
{% block stylesheets %}
 <link rel="stylesheet" href="{{ asset('css/recipes.css') }}">
 <link rel="stylesheet" href="{{asset('css/style.css')}}"> 
  
 {% endblock %}
{% block javascripts %}
<script src="{{ asset('js/recipes.js') }}"></script>
{% endblock %}
{% block body %}

<div class="capsule">
        
        <a href="#" onclick="beep()"><img src="{{asset('images/accueil.png')}}" class="img-fluid"></a>
        <a href="{{ path('beers_index') }}" onclick="beep()"><img src="{{asset('images/bieres.png')}}" class="img-fluid"></a>
        <a href="{{ path('recipes_index') }}" onclick="beep()"><img src="{{asset('images/recettes.png')}}" class="img-fluid"></a>
        <a href="{{ path('quizz') }}" onclick="beep()"><img src="{{asset('images/quiz.png')}}" class="img-fluid"></a>
        <a href="{{ path('infos') }}" onclick="beep()"><img src="{{asset('images/infos.png')}}" class="img-fluid"></a>
  
    </div><center>
    <img src="{{ asset('images/beer-svgrepo-com.svg')}}" id="js-arrow" style="height:50vh">
    
    <h1>Recipes</h1>
    </center>
<div class="container recette">
    <table class="table">
        <tbody>
            {# <tr> #}
                {# <th>Id</th> #}
                {# <td>{{ recipe.id }}</td> #}
            {# </tr> #}

<tr>
                <th class="text-center"></th>
                <td class="text-center">{{ recipe.name|upper }}</td>
            </tr>
            <tr>
                <th class="text-center"></th>
                <td class="text-center"><img src="{{recipe.photo}}"> </td>
            </tr>
            
            <tr>
                <th>Ingredients</th>
                <td>{{ recipe.ingredients|nl2br }}</td>
            </tr>
            <tr>
                <th>Recette</th>
                <td>{{ recipe.recipe|nl2br }}</td>
            </tr>
            
            <tr>
                <th>Comments</th>
                <td>{{ recipe.comments }}</td>
            </tr>
        </tbody>
    </table>
</div>
    <a href="{{ path('recipes_index') }}">back to list</a>

    <a href="{{ path('recipes_edit', {'id': recipe.id}) }}">edit</a>

    {{ include('recipes/_delete_form.html.twig') }}

{% endblock %}
